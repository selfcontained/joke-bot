name: roflbot
description: roflbot will share jokes when prompted, as well as chime in when he thinks it's appropriate to try and keep the lols and rofls flowing.
avatar: images/avatar.jpg
background: images/background.jpg
screenshots:
  - screenshot: images/screenshot.png
  - screenshot: images/screenshot2.png
config:
  - name: NODE_ENV
    global: true
  - name: REDIS_HOST
    global: true
    type: secret
  - name: REDIS_PORT
    global: true
    type: secret
  - name: REDIS_PASS
    global: true
    type: secret
  - name: MIXPANEL_TOKEN
    global: true
    type: secret
